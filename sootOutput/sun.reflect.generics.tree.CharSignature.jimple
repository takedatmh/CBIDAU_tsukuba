public class sun.reflect.generics.tree.CharSignature extends java.lang.Object implements sun.reflect.generics.tree.BaseType
{
    private static sun.reflect.generics.tree.CharSignature singleton;

    private void <init>()
    {
        sun.reflect.generics.tree.CharSignature r0;

        r0 := @this: sun.reflect.generics.tree.CharSignature;
        specialinvoke r0.<java.lang.Object: void <init>()>();
        return;
    }

    public static sun.reflect.generics.tree.CharSignature make()
    {
        sun.reflect.generics.tree.CharSignature $r0;

        $r0 = <sun.reflect.generics.tree.CharSignature: sun.reflect.generics.tree.CharSignature singleton>;
        return $r0;
    }

    public void accept(sun.reflect.generics.visitor.TypeTreeVisitor)
    {
        sun.reflect.generics.tree.CharSignature r0;
        sun.reflect.generics.visitor.TypeTreeVisitor r1;

        r0 := @this: sun.reflect.generics.tree.CharSignature;
        r1 := @parameter0: sun.reflect.generics.visitor.TypeTreeVisitor;
        interfaceinvoke r1.<sun.reflect.generics.visitor.TypeTreeVisitor: void visitCharSignature(sun.reflect.generics.tree.CharSignature)>(r0);
        return;
    }

    static void <clinit>()
    {
        sun.reflect.generics.tree.CharSignature $r0;

        $r0 = new sun.reflect.generics.tree.CharSignature;
        specialinvoke $r0.<sun.reflect.generics.tree.CharSignature: void <init>()>();
        <sun.reflect.generics.tree.CharSignature: sun.reflect.generics.tree.CharSignature singleton> = $r0;
        return;
    }
}
